/* eslint @typescript-eslint/no-unused-vars: "off" */

const enum Key {
    VERSION = 'version',
    VOLUME = 'lastVolume',
    CACHE = 'cache',
    CONFIG = 'config',
    GUEST_TOKEN = 'GUEST_TOKEN', // cache.GUEST_TOKEN
    USE_GUEST = 'use_guest',
}
